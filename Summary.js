// 1. npm init --yes -> project's default metadata

// 2. Install express (and nodemon)
// > const express = require("express");
// > const app = express(); // returns an object of type Express

// 3. Building a simple server
// app.get("/", (req, res) => {
//   res.send("Some JSON object");
// });
// app.listen(portNumber, () => console.log(`Listening to ${portNumber}`))
 
// 3. Install mongoose