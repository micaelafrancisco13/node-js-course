// "npm outdated" returns the outdated packages and their
// required and latest versions

// Update minor and patch releases via "npm update"
// To update a package to a major release, run 
// "npm i -g npm-check-updates". This will install a new 
// package. Run "ncu -u" to update to a major release.

// Note that by running "ncu -u," the dependency is not yet
// updated, only package.json. To fix this, run "npm i"