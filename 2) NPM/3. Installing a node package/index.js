// npm i lodash
// When you run this, two things happen. First, in 
// package.JSON, you can see this new property: 
// "dependencies," and under that, you can see lodash.

// When you run "npm install," NPM is going to download the 
// latest version of the given package from NPM registry, 
// and then it will store it inside a folder called 
// "node_modules"

// Under the folder "lodash" inside the node_modules, one 
// of the files is called "package.json." Every node module 
// has this package.json just like our node application.

// node_modules > lodash > package.json
