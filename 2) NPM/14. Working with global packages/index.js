// There are node packages on NPM registry that are not
// specific to an application. These are often command line
// tools that you want to access from everywhere. They're
// not tied to a specific folder or a specific project. NPM
// is an example of one of these global packages. It's a
// command line tool we can run from any folders. It's not
// specific to a given project.

// installing a node package globally...
// "npm i -g package_name"
// All the other NPM commands that you have learned 
// throughout this section, they also work with global 
// packages.