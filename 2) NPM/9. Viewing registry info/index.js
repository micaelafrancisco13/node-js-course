// View all the metadata of a package (package.json file) 
// via the command "npm view package_name"

// View all the dependencies of a package via the command 
// "npm view package_name dependencies" 

// Versions of a package that have been released so far via
// the command  "npm view package_name versions"

// Any properties inside that package.json file
// "npm view package_name propertyName"